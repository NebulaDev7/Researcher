# All keys are optional — Researcher works out of the box.

# Tavily fallback (only used if the free sources come up short)
# Get a key at https://app.tavily.com
TAVILY_API_KEY=

# StackExchange (raises the API rate limit from ~300/day)
# Get a key at https://stackapps.com/apps/oauth/register
STACKEXCHANGE_KEY=
